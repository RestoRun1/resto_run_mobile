import 'package:flutter/material.dart';
import 'package:resto_run_mobile/Models/reservation.dart';

class PastWidget extends StatelessWidget {
  final Reservation reservation;

  PastWidget({required this.reservation});

  @override
  Widget build(BuildContext context) {
    // Use details from the reservation to build the past state UI
    return Column(
      // Populate the widget with reservation details
    );
  }
}