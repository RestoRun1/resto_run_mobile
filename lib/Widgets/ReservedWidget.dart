import 'package:flutter/material.dart';
import 'package:resto_run_mobile/Models/reservation.dart';

class ReservedWidget extends StatelessWidget {
  final Reservation reservation;

  ReservedWidget({required this.reservation});

  @override
  Widget build(BuildContext context) {
    // Use details from the reservation to build the reserved state UI
    return Column(
      // Populate the widget with reservation details
    );
  }
}