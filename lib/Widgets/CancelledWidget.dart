import 'package:flutter/material.dart';
import 'package:resto_run_mobile/Models/reservation.dart';

class CancelledWidget extends StatelessWidget {
  final Reservation reservation;

  CancelledWidget({required this.reservation});

  @override
  Widget build(BuildContext context) {
    // Use details from the reservation to build the cancelled state UI
    return Column(
      // Populate the widget with reservation details
    );
  }
}